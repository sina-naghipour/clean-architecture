[project]
name = "kata_refactors"
version = "0.1.0"
description = "Kata refactoring exercises"
readme = "README.md"
requires-python = ">=3.11"
authors = [{ name = "Sina" }]

[tool.setuptools.packages.find]
where = ["src"]
